--[[ 
    XENOHUB ULTIMATE - BROOKHAVEN MEGA FULL SCRIPT
    Geliştirici: XenoHub Team & EgeKazak_Real1
    Özellikler: Orijinal ModalFireAsk, ADStore & VideoAd Tetikleyici, Premium Unlock, Max Bypass
    Version: 8.0 (Decompiled Ad-System Integrated)
]]

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local StarterGui = game:GetService("StarterGui")

local lp = Players.LocalPlayer
local PlayerGui = lp:WaitForChild("PlayerGui")
local ParentFolder = (gethui and gethui()) or CoreGui

-- ============================================================================== --
-- 1. TEMEL AYARLAR & REKLAM SISTEMI ENTEGRASYONU
-- ============================================================================== --

local XenoSettings = {
    PremiumEnabled = false,
    Speed = 16,
    Jump = 50,
    Noclip = false
}

local function SendNotif(title, text)
    pcall(function()
        StarterGui:SetCore("SendNotification", {
            Title = title;
            Text = text;
            Duration = 5;
        })
    end)
end

-- ============================================================================== --
-- 2. ORIJINAL REKLAM & MAGAZA BYPASS (DEX LOGIC)
-- ============================================================================== --

local function TriggerVideoAdSystem()
    pcall(function()
        -- Decompiled kodlardaki AdvertisementsController ve PanelController erişimi
        local AdsController = require(ReplicatedStorage.Modules.Client.Ads.AdvertisementsController)
        local PanelController = require(ReplicatedStorage.Modules.Client.UI.PanelController)
        
        -- AdStore ve Ads butonlarını bul ve DEX gibi zorla aç
        local MainGUI = PlayerGui:FindFirstChild("MainGUIHandler")
        if MainGUI then
            local adStore = MainGUI:FindFirstChild("AdStore", true)
            local shopButtons = MainGUI:FindFirstChild("ShopButtons", true)
            
            if shopButtons then
                local adsBtn = shopButtons:FindFirstChild("Ads", true) or shopButtons:FindFirstChild("AdsButton", true)
                if adsBtn then
                    adsBtn.Visible = true
                    adsBtn.Active = true
                end
            end
            
            if adStore then
                adStore.Visible = true
                adStore.Active = true
                SendNotif("XenoHub", "ADStore Paneli Zorla Açıldı! (Dex Logic)")
            end
        end

        -- Reklam Hazırlığı ve Tetikleme (Decompiled Start mantığı)
        -- Normalde "Ad not available" hatası veren kontrolleri bypass ediyoruz
        SendNotif("XenoHub", "Reklam Sistemleri Bypass Ediliyor...")
        
        -- Orijinal RequestRewardedVideoAd fonksiyonunu kullanarak sistemi tetikle
        -- Args: itemId, icon, gamepassId, category, action, boolean, boolean
        AdsController.RequestRewardedVideoAd(
            "XenoBypass", 
            "rbxassetid://0", 
            0, 
            "Item Refresh", 
            "Item Refresh", 
            false, 
            false
        )
        
        SendNotif("XenoHub", "Reklam Tetiklendi! (PC/Mobil Uyumlu)")
    end)
end

-- ============================================================================== --
-- 3. DIGER ORIJINAL GUI TETİKLEYİCİLER
-- ============================================================================== --

local function OpenOriginalFireModal()
    pcall(function()
        local originalModal = nil
        for _, v in pairs(PlayerGui:GetDescendants()) do
            if v.Name == "ModalFireAsk" and (v:IsA("Frame") or v:IsA("ImageLabel")) then
                originalModal = v
                break
            end
        end

        if originalModal then
            originalModal.Visible = true
            originalModal.Active = true
            
            for _, btn in pairs(originalModal:GetDescendants()) do
                if btn:IsA("GuiButton") then
                    if btn.Name:lower():find("yes") or btn.BackgroundColor3 == Color3.fromRGB(34, 139, 34) then
                        btn.MouseButton1Click:Connect(function()
                            local Game8 = require(StarterGui.Player8Handler.Game8Settings)
                            Game8.PlayersHouse:FireServer("PlayerWantsFire")
                            SendNotif("XenoHub", "Orijinal Yangın Tetiklendi! 🔥")
                        end)
                    end
                end
            end
        else
            local Game8 = require(StarterGui.Player8Handler.Game8Settings)
            Game8.PlayersHouse:FireServer("PlayerWantsFire")
            SendNotif("XenoHub", "Remote Yangın Tetiklendi.")
        end
    end)
end

-- ============================================================================== --
-- 4. MEGA UI TASARIMI
-- ============================================================================== --

if ParentFolder:FindFirstChild("XenoBrookhaven") then ParentFolder.XenoBrookhaven:Destroy() end

local XenoGui = Instance.new("ScreenGui")
XenoGui.Name = "XenoBrookhaven"
XenoGui.Parent = ParentFolder
XenoGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 700, 0, 480)
MainFrame.Position = UDim2.new(0.5, -350, 0.5, -240)
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 12)
MainFrame.Parent = XenoGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(145, 90, 230)
MainStroke.Thickness = 2
MainStroke.Parent = MainFrame

local function MakeDraggable(f)
    local dragging, dragInput, dragStart, startPos
    f.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true dragStart = input.Position startPos = f.Position
            input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end)
        end
    end)
    f.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end
    end)
    RunService.RenderStepped:Connect(function()
        if dragging and dragInput then
            local delta = dragInput.Position - dragStart
            f.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end
MakeDraggable(MainFrame)

-- ============================================================================== --
-- 5. SAYFALAR & ÖZELLİKLER (SIDEBAR)
-- ============================================================================== --

local SideBar = Instance.new("Frame")
SideBar.Size = UDim2.new(0, 180, 1, 0)
SideBar.BackgroundColor3 = Color3.fromRGB(15, 15, 18)
SideBar.Parent = MainFrame
Instance.new("UICorner", SideBar).CornerRadius = UDim.new(0, 12)

local TabHolder = Instance.new("Frame")
TabHolder.Size = UDim2.new(1, -200, 1, -20)
TabHolder.Position = UDim2.new(0, 190, 0, 10)
TabHolder.BackgroundTransparency = 1
TabHolder.Parent = MainFrame

local pages = {}
local function NewTab(name)
    local tBtn = Instance.new("TextButton")
    tBtn.Size = UDim2.new(0.9, 0, 0, 40)
    tBtn.Position = UDim2.new(0.05, 0, 0, 60 + (#pages * 45))
    tBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
    tBtn.BackgroundTransparency = 1
    tBtn.Text = "  " .. name
    tBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
    tBtn.Font = Enum.Font.GothamBold
    tBtn.TextSize = 13
    tBtn.TextXAlignment = Enum.TextXAlignment.Left
    tBtn.Parent = SideBar
    Instance.new("UICorner", tBtn).CornerRadius = UDim.new(0, 6)

    local page = Instance.new("ScrollingFrame")
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.Visible = false
    page.ScrollBarThickness = 2
    page.Parent = TabHolder
    Instance.new("UIListLayout", page).Padding = UDim.new(0, 8)

    tBtn.MouseButton1Click:Connect(function()
        for _, p in pairs(pages) do p.p.Visible = false p.b.BackgroundTransparency = 1 p.b.TextColor3 = Color3.fromRGB(200, 200, 200) end
        page.Visible = true
        tBtn.BackgroundTransparency = 0.8
        tBtn.TextColor3 = Color3.fromRGB(145, 90, 230)
    end)

    if #pages == 0 then page.Visible = true tBtn.BackgroundTransparency = 0.8 tBtn.TextColor3 = Color3.fromRGB(145, 90, 230) end
    table.insert(pages, {b = tBtn, p = page})
    return page
end

local function AddBtn(parent, text, cb, color)
    local b = Instance.new("TextButton")
    b.Size = UDim2.new(0.96, 0, 0, 44)
    b.BackgroundColor3 = color or Color3.fromRGB(25, 25, 30)
    b.Text = text
    b.TextColor3 = Color3.new(1, 1, 1)
    b.Font = Enum.Font.GothamBold
    b.TextSize = 13
    b.Parent = parent
    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 6)
    b.MouseButton1Click:Connect(cb)
end

-- ============================================================================== --
-- 6. ANA SAYFA & OZELLIKLER
-- ============================================================================== --

local Home = NewTab("🏠 Ana Sayfa")

AddBtn(Home, "📺 REKLAM & ADSTORE TETIKLE (FULL BYPASS)", function()
    TriggerVideoAdSystem()
end, Color3.fromRGB(0, 162, 255))

AddBtn(Home, "🔥 MODALFIREASK AÇ (DEX TRUE)", function()
    OpenOriginalFireModal()
end, Color3.fromRGB(255, 80, 0))

AddBtn(Home, "🔓 PREMIUM UNLOCK (Max Bypass)", function()
    pcall(function()
        XenoSettings.PremiumEnabled = true
        local gpc = require(ReplicatedStorage.Modules.Client.UI.GamepassUnlockController)
        gpc.OwnsGamepass = function() return true end
        gpc.IsPremium = function() return true end
        SendNotif("XenoHub", "Premium Dex Modu Aktif! ✅")
    end)
end, Color3.fromRGB(145, 90, 230))

local Player = NewTab("🏃 Oyuncu")
AddBtn(Player, "⚡ Speed (100)", function() lp.Character.Humanoid.WalkSpeed = 100 end)
AddBtn(Player, "🚀 Jump (150)", function() lp.Character.Humanoid.JumpPower = 150 end)

local Items = NewTab("🎒 Eşyalar")
local function SafeTriggerTool(tool)
    pcall(function()
        local re = ReplicatedStorage:FindFirstChild("RE")
        local event = re and re:FindFirstChild("1Playe1rTrigge1rEven1t")
        if event then event:FireServer("AcceptedToolToServer", tool, lp) end
    end)
end

local items_list = {
    {"MoneyGun", "Para Tabancası"}, {"Glider", "Planör"}, {"Chainsaw", "Testere"},
    {"Scythe", "Tırpan"}, {"UraniumRod", "Uranyum"}, {"AgencyBook", "Ajans Kitabı"}
}
for _, item in pairs(items_list) do
    AddBtn(Items, "🛠️ Get: " .. item[2], function() SafeTriggerTool(item[1]) end)
end

local Settings = NewTab("⚙️ Ayarlar")
AddBtn(Settings, "❌ SCRIPTI KAPAT", function() XenoGui:Destroy() end, Color3.fromRGB(100, 20, 20))

-- TOGGLE BUTTON
local TButton = Instance.new("TextButton")
TButton.Size = UDim2.new(0, 60, 0, 60)
TButton.Position = UDim2.new(0, 40, 0.5, -30)
TButton.BackgroundColor3 = Color3.fromRGB(12, 12, 15)
TButton.Text = "X"
TButton.TextColor3 = Color3.fromRGB(145, 90, 230)
TButton.Font = Enum.Font.GothamBlack
TButton.TextSize = 25
TButton.Parent = XenoGui
Instance.new("UICorner", TButton).CornerRadius = UDim.new(1, 0)
Instance.new("UIStroke", TButton).Color = Color3.fromRGB(145, 90, 230)
MakeDraggable(TButton)

TButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
    TButton.Text = MainFrame.Visible and "X" or "O"
end)

SendNotif("XenoHub ULTIMATE", "Reklam & AdStore Entegrasyonu Başarılı!")
