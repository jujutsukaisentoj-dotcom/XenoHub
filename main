-- =========================================================
-- BUILD A BOAT - FULL TETİKLİ & YAVAŞ SAFE FARMER (V6)
-- =========================================================

local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("BABFT - Pro Tetikli v6", "Midnight")

-- SERVICES & DATA
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Data = LocalPlayer:WaitForChild("OtherData")
local Claim = workspace:WaitForChild("ClaimRiverResultsGold")
local StartGold = LocalPlayer.Data.Gold.Value
local GainedGold = 0
local Chests = 0
local StartTime = os.time()
local AutoFarmActive = false 

-- MOBILE UI TOGGLE
local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
local ToggleBtn = Instance.new("TextButton", ScreenGui)
ToggleBtn.Size = UDim2.new(0, 90, 0, 35)
ToggleBtn.Position = UDim2.new(0, 10, 0, 250)
ToggleBtn.Text = "MENU"
ToggleBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 255)
ToggleBtn.TextColor3 = Color3.new(1,1,1)
ToggleBtn.Draggable = true
ToggleBtn.MouseButton1Click:Connect(function() Library:ToggleUI() end)

-- SEKMELER
local MainTab = Window:NewTab("Farming")
local StatusSection = MainTab:NewSection("Anlık Veriler")

-- UI SAYAÇLARI (TETİKLİ)
local goldLabel = StatusSection:NewLabel("Currenct Gold: " .. StartGold)
local gainedLabel = StatusSection:NewLabel("Gained Gold: 0")
local chestLabel = StatusSection:NewLabel("Chest Collected: 0")
local timeLabel = StatusSection:NewLabel("Total Time Farming: 00:00:00")
local stageLabel = StatusSection:NewLabel("Durum: Bekleniyor...")

-- STAGE ONAY FONKSİYONU
local function HasStage(Num)
    return Data[`Stage{Num}`].Value ~= "" and Data[`Stage{Num}`].Value
end

-- YAVAŞ VE TETİKLİ FARM DÖNGÜSÜ
local function StartSlowFarm()
    while AutoFarmActive do
        local Char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
        local Root = Char:WaitForChild("HumanoidRootPart")
        local Stages = workspace.BoatStages.NormalStages
        
        -- 1'den 10'a Kadar Bölüm Bölüm Geçiş
        for i = 1, 10 do
            if not AutoFarmActive then break end
            
            stageLabel:UpdateLabel("Bölüm Geçiliyor: " .. i .. " / 11")
            
            local Target = Stages[`CaveStage{i}`].DarknessPart
            Root.CFrame = Target.CFrame
            Root.Velocity = Vector3.zero
            
            -- Sunucuya Bildirim Gönder (Claim)
            task.spawn(function() Claim:FireServer() end)
            
            -- FULL YAVAŞ: 4.5 Saniye Bekle (Anticheat Bypass)
            task.wait(4.5) 
            
            -- Bölümün Kaydedilmesini Bekle (Tetikli)
            repeat task.wait(0.5) until HasStage(i) or not AutoFarmActive
        end

        -- 11. BÖLÜM (THE END - SANDIK)
        if AutoFarmActive then
            stageLabel:UpdateLabel("Durum: SANDIK ALINIYOR (11/11)")
            local EndStage = Stages.TheEnd.GoldenChest
            Root.CFrame = EndStage.Trigger.CFrame
            task.wait(2)
            
            -- Sandık Dokunma Bypass
            EndStage.Trigger.CFrame = Root.CFrame
            task.wait(2)
            
            -- Sandık Sayacını Tetikle
            Chests = Chests + 1
            chestLabel:UpdateLabel("Chest Collected: " .. Chests)
            
            -- Reset ve Güvenli Bekleme
            stageLabel:UpdateLabel("Durum: Resetleniyor, Bekle...")
            Char:BreakJoints()
            task.wait(8) 
        end
    end
end

-- KONTROL BUTONU
StatusSection:NewToggle("Auto Farm [BAŞLAT]", "Bölümleri yavaşça geçer", function(state)
    AutoFarmActive = state
    if state then
        Library:Notify("Sistem", "Farm Başlatıldı!", "Default")
        task.spawn(StartSlowFarm)
    else
        Library:Notify("Sistem", "Farm Durduruldu.", "Default")
    end
end)

-- DOUBLE GOLD BUTTON
StatusSection:NewButton("Double Gold (X2) [TETİKLE]", "DEX: TRUE / Bypass", function()
    local gp = Instance.new("BoolValue", LocalPlayer); gp.Name = "GoldPass"
    Library:Notify("Gamepass", "Double Gold Aktif Edildi!", "Success")
end)

-- CANLI TAKİP DÖNGÜSÜ
task.spawn(function()
    while task.wait(1) do
        -- Zaman Sayacı
        local diff = os.time() - StartTime
        local h, m, s = math.floor(diff/3600), math.floor((diff%3600)/60), diff%60
        timeLabel:UpdateLabel("Total Time Farming: " .. string.format("%02d:%02d:%02d", h, m, s))
        
        -- Altın Verisi
        local cur = LocalPlayer.Data.Gold.Value
        goldLabel:UpdateLabel("Currenct Gold: " .. cur)
        gainedLabel:UpdateLabel("Gained Gold: " .. (cur - StartGold))
    end
end)
