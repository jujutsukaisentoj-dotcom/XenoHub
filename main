-- ===============================================================
-- 👑 GEMINI HUB V10 - ULTIMATE BUILD A BOAT FARM
-- 🚀 STABİL SIRALI GEÇİŞ + SUNUCU ONAYLI SİSTEM
-- ===============================================================

local Settings = {
    FarmEnabled = false,
    DoubleGold = true,
    AntiWater = true,
    MaxStages = 10, -- 1-10 arası bölgeler
    ChestStage = 11, -- 11. aşama sandık
    TeleportSpeed = 0.1, -- Güvenli bekleme süresi
    AutoClaim = true
}

-- Servisler
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LocalPlayer = Players.LocalPlayer
local Data = LocalPlayer:WaitForChild("OtherData")
local Claim = Workspace:WaitForChild("ClaimRiverResultsGold")
local Stages = Workspace:WaitForChild("BoatStages"):WaitForChild("NormalStages")

-- [ 2X GOLD & DATA BYPASS ]
local function EnableDoubleGold()
    if Settings.DoubleGold then
        pcall(function()
            if Data:FindFirstChild("DoubleGold") then
                Data.DoubleGold.Value = true
            end
            if ReplicatedStorage:FindFirstChild("DoubleGoldGamepass") then
                ReplicatedStorage.DoubleGoldGamepass:FireServer(true)
            end
        end)
    end
end

-- [ GUI OLUŞTURMA ]
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "Gemini_V10"
ScreenGui.Parent = (gethui and gethui()) or (game:GetService("CoreGui")) or LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
MainFrame.Size = UDim2.new(0, 220, 0, 260)
MainFrame.Position = UDim2.new(0.05, 0, 0.3, 0)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 12)
UICorner.Parent = MainFrame

local Title = Instance.new("TextLabel")
Title.Parent = MainFrame
Title.Size = UDim2.new(1, 0, 0, 45)
Title.Text = "GEMINI HUB V10 👑"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 16
Title.BackgroundColor3 = Color3.fromRGB(30, 0, 100)

local Corner2 = Instance.new("UICorner")
Corner2.CornerRadius = UDim.new(0, 12)
Corner2.Parent = Title

-- [ BUTONLAR ]
local function CreateButton(name, pos, text, callback)
    local Btn = Instance.new("TextButton")
    Btn.Name = name
    Btn.Parent = MainFrame
    Btn.Position = pos
    Btn.Size = UDim2.new(0.9, 0, 0, 38)
    Btn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    Btn.Text = text
    Btn.TextColor3 = Color3.fromRGB(220, 220, 220)
    Btn.Font = Enum.Font.GothamMedium
    Btn.TextSize = 13
    Btn.BorderSizePixel = 0
    
    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 8)
    Corner.Parent = Btn
    
    Btn.Activated:Connect(function()
        callback(Btn)
    end)
    return Btn
end

-- Farm Butonu
local FarmBtn = CreateButton("FarmBtn", UDim2.new(0.05, 0, 0.22, 0), "AUTO FARM: KAPALI", function(self)
    Settings.FarmEnabled = not Settings.FarmEnabled
    if Settings.FarmEnabled then
        self.Text = "AUTO FARM: AÇIK"
        self.BackgroundColor3 = Color3.fromRGB(0, 180, 80)
        self.TextColor3 = Color3.fromRGB(255, 255, 255)
    else
        self.Text = "AUTO FARM: KAPALI"
        self.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
        self.TextColor3 = Color3.fromRGB(220, 220, 220)
    end
end)

-- Double Gold Butonu
local GoldBtn = CreateButton("GoldBtn", UDim2.new(0.05, 0, 0.40, 0), "2X GOLD: AKTİF (BYPASS)", function(self)
    Settings.DoubleGold = not Settings.DoubleGold
    if Settings.DoubleGold then
        self.Text = "2X GOLD: AKTİF (BYPASS)"
        self.BackgroundColor3 = Color3.fromRGB(220, 160, 0)
        EnableDoubleGold()
    else
        self.Text = "2X GOLD: PASİF"
        self.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    end
end)
GoldBtn.BackgroundColor3 = Color3.fromRGB(220, 160, 0)

-- Anti Water Butonu
CreateButton("WaterBtn", UDim2.new(0.05, 0, 0.58, 0), "ANTİ-SU HASARI: AÇIK", function(self)
    Settings.AntiWater = not Settings.AntiWater
    self.Text = Settings.AntiWater and "ANTİ-SU HASARI: AÇIK" or "ANTİ-SU HASARI: KAPALI"
end)

-- [ FONKSİYONLAR ]

local function HasStage(Num)
    local val = Data:FindFirstChild(`Stage{Num}`)
    -- Değerin boş olmaması sunucunun o bölgeyi onayladığı anlamına gelir
    return val and val.Value ~= "" and val.Value
end

local function ClaimStage(Num)
    if not Settings.AutoClaim then return end
    -- Belirli bir bölge için onay bekleme mantığı
    task.spawn(function()
        local Value = Data:FindFirstChild(`Stage{Num}`)
        if Value and Value.Value == "" then
            Value:GetPropertyChangedSignal("Value"):Wait()
        end
        Claim:FireServer()
    end)
end

local function FarmLoop()
    while true do
        if Settings.FarmEnabled then
            local Char = LocalPlayer.Character
            local Root = Char and Char:FindFirstChild("HumanoidRootPart")
            
            if Root then
                EnableDoubleGold()
                
                local NewChar = false
                local charConn
                charConn = LocalPlayer.CharacterAdded:Once(function()
                    NewChar = true
                end)

                -- Karakteri sabitle (Titremeyi önler)
                local BV = Instance.new("BodyVelocity", Root)
                BV.Velocity = Vector3.new(0,0,0)
                
                -- SIRALI VE SAKİN GEÇİŞ (1-10)
                for i = 1, Settings.MaxStages do
                    if not Settings.FarmEnabled or NewChar then break end
                    
                    local Stage = Stages:FindFirstChild(`CaveStage{i}`)
                    if Stage and Stage:FindFirstChild("DarknessPart") then
                        local TargetPart = Stage.DarknessPart
                        local Teleporting = true
                        
                        -- Karakteri bölgeye kilitle (Sunucu onaylayana kadar)
                        task.spawn(function()
                            while Teleporting and Settings.FarmEnabled and not NewChar do
                                Root.CFrame = TargetPart.CFrame
                                -- Fiziksel hızları sıfırla (Anti-Cheat bypass)
                                for _, part in pairs(Char:GetChildren()) do
                                    if part:IsA("BasePart") then
                                        part.Velocity = Vector3.zero
                                        part.RotVelocity = Vector3.zero
                                    end
                                end
                                task.wait()
                            end
                        end)
                        
                        -- Bölgeyi talep et ve sunucu yanıtını bekle
                        ClaimStage(i)
                        
                        -- ÇOK ÖNEMLİ: Sunucu Stage{i} değerini güncelleyene kadar bekle
                        -- Bu sayede farm "fazla hızlı" olmaz, sırayla ve güvenli gider.
                        local timeout = tick()
                        repeat task.wait() until HasStage(i) or NewChar or not Settings.FarmEnabled or (tick() - timeout > 4)
                        
                        Teleporting = false
                        task.wait(Settings.TeleportSpeed) -- Kısa bir nefes payı
                    end
                end
                
                -- SANDIK ALMA (11. Aşama)
                if Settings.FarmEnabled and not NewChar then
                    local EndStage = Stages:FindFirstChild("TheEnd")
                    if EndStage and EndStage:FindFirstChild("GoldenChest") then
                        local Chest = EndStage.GoldenChest.Trigger
                        
                        -- Sandığı karakterin üzerine çekme (Xeno/Solara Bypass)
                        local OldCFrame = Chest.CFrame
                        Chest.CFrame = Root.CFrame
                        
                        if firetouchinterest then
                            firetouchinterest(Root, Chest, 0)
                            task.wait(0.1)
                            firetouchinterest(Root, Chest, 1)
                        end
                        
                        task.wait(0.3)
                        Chest.CFrame = OldCFrame -- Sandığı orijinal yerine çek
                        
                        Claim:FireServer()
                        task.wait(1.5)
                        Char:BreakJoints() -- Yeni döngü için reset
                    end
                end
                
                if BV then BV:Destroy() end
                if charConn then charConn:Disconnect() end
            end
        end
        task.wait(1)
    end
end

-- Su hasarını engelleme
RunService.Stepped:Connect(function()
    if Settings.AntiWater and LocalPlayer.Character then
        pcall(function()
            for _, v in pairs(LocalPlayer.Character:GetChildren()) do
                if v:IsA("BasePart") then v.CanTouch = false end
            end
        end)
    end
end)

task.spawn(FarmLoop)
print("🚀 Gemini Hub V10 - Stabil Onaylı Döngü Aktif!")
