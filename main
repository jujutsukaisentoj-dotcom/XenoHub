--[[
	ULTIMATE BUILD A BOAT FOR TREASURE - FULL BYPASS & FARM (V4.1)
	- İlk sürümün hız ve mantığı korundu.
	- Sol Alt Köşe Menü Tetikleyici (Toggle Button) eklendi.
	- Gelişmiş Meta-Table Bypass aktif edildi.
	- Hız, Zıplama, Yerçekimi ve Noclip eklendi.
	- Otomatik Farm & Otomatik Market entegre edildi.
]]

local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/shlexware/Orion/main/source')))()
local Window = OrionLib:MakeWindow({Name = "BaBft Ultimate - Pro Edition", HidePremium = false, SaveConfig = true, ConfigFolder = "BaBftUltimate"})

-- ================================
-- Servisler ve Değişkenler
-- ================================
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Workspace = game:GetService("Workspace")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")

local farming = false
local autoBuyType = "None"
local walkSpeed = 16
local jumpPower = 50
local noclipActive = false

-- ================================
-- SOL ALT MENÜ BUTONU (GUI)
-- ================================
local ScreenGui = Instance.new("ScreenGui")
local OpenBtn = Instance.new("TextButton")
local UICorner = Instance.new("UICorner")
local UIStroke = Instance.new("UIStroke")

ScreenGui.Name = "MenuTrigger"
ScreenGui.Parent = CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

OpenBtn.Name = "OpenBtn"
OpenBtn.Parent = ScreenGui
OpenBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
OpenBtn.BorderSizePixel = 0
OpenBtn.Position = UDim2.new(0, 15, 1, -70)
OpenBtn.Size = UDim2.new(0, 60, 0, 60)
OpenBtn.Font = Enum.Font.GothamBold
OpenBtn.Text = "HİLE"
OpenBtn.TextColor3 = Color3.fromRGB(0, 255, 127)
OpenBtn.TextSize = 14.000

UICorner.CornerRadius = UDim.new(0, 12)
UICorner.Parent = OpenBtn

UIStroke.Parent = OpenBtn
UIStroke.Thickness = 2
UIStroke.Color = Color3.fromRGB(0, 255, 127)
UIStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

OpenBtn.MouseButton1Click:Connect(function()
    local frame = CoreGui:FindFirstChild("Orion")
    if frame then
        frame.Enabled = not frame.Enabled
    end
end)

-- ================================
-- FULL BYPASS MANTIĞI
-- ================================
local function ApplyBypass()
    local mt = getrawmetatable(game)
    local oldNamecall = mt.__namecall
    setreadonly(mt, false)

    mt.__namecall = newcclosure(function(self, ...)
        local method = getnamecallmethod()
        
        if method == "Kick" or method == "kick" then
            return nil
        end
        
        if not checkcaller() and (method == "FireServer" or method == "InvokeServer") then
            local name = tostring(self)
            if name:find("Report") or name:find("Cheat") or name:find("Detection") then
                 OrilLib:MakeNotification({Name = "Bypass", Content = "Raporlama engellendi.", Time = 2})
                return nil
            end
        end

        return oldNamecall(self, ...)
    end)

    setreadonly(mt, true)
end

ApplyBypass()

-- ================================
-- Fonksiyonlar
-- ================================
local function ClearMap()
    pcall(function()
        local stages = Workspace:WaitForChild("BoatStages"):WaitForChild("NormalStages")
        for _, stage in pairs(stages:GetChildren()) do
            if stage:FindFirstChild("DarknessPart") then
                stage.DarknessPart:Destroy()
            end
        end
    end)
end

RunService.Stepped:Connect(function()
    if noclipActive and LocalPlayer.Character then
        for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = false
            end
        end
    end
end)

-- ================================
-- Farm Mantığı (İlk Sürüm Mantığıyla)
-- ================================
local function StartFarm()
    task.spawn(function()
        while farming do
            local char = LocalPlayer.Character
            if not char or not char:FindFirstChild("HumanoidRootPart") then
                LocalPlayer.CharacterAdded:Wait()
                task.wait(1)
                char = LocalPlayer.Character
            end

            local root = char:WaitForChild("HumanoidRootPart")
            ClearMap()
            
            -- İlk sürümdeki gibi yerçekimini etkisiz kılan velocity
            local bv = Instance.new("BodyVelocity")
            bv.Name = "FarmVelocity"
            bv.Velocity = Vector3.new(0, 0, 0)
            bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
            bv.Parent = root

            local endZone = Workspace.BoatStages.NormalStages.TheEnd.GoldenChest.Trigger
            
            -- Hızlı ve güvenli geçiş rotası
            local path = {
                CFrame.new(-55, 85, 1500),
                CFrame.new(-55, 85, 4500),
                CFrame.new(-55, 85, 7500),
                CFrame.new(-55, 85, 9450),
                endZone.CFrame
            }

            for _, targetCF in ipairs(path) do
                if not farming then break end
                local dist = (root.Position - targetCF.Position).Magnitude
                -- Hız 350 olarak ayarlandı (İlk sürümün akıcılığında)
                local tween = TweenService:Create(root, TweenInfo.new(dist / 350, Enum.EasingStyle.Linear), {CFrame = targetCF})
                tween:Play()
                tween.Completed:Wait()
            end

            if farming then
                root.CFrame = endZone.CFrame
                task.wait(10) -- Sandık onayı için bekleme
                bv:Destroy()
                char:BreakJoints()
                task.wait(4)
            end
        end
    end)
end

-- ================================
-- Arayüz Sayfaları
-- ================================
local MainTab = Window:MakeTab({Name = "Ana Menü", Icon = "rbxassetid://4483345998"})
local PlayerTab = Window:MakeTab({Name = "Karakter", Icon = "rbxassetid://4483345998"})
local ShopTab = Window:MakeTab({Name = "Market", Icon = "rbxassetid://4483345998"})

MainTab:AddToggle({
	Name = "Otomatik Farm Başlat",
	Default = false,
	Callback = function(Value)
		farming = Value
		if farming then StartFarm() end
	end    
})

MainTab:AddButton({
	Name = "Bypass Duvarları Sil",
	Callback = function() ClearMap() end
})

PlayerTab:AddSlider({
	Name = "Yürüme Hızı",
	Min = 16,
	Max = 400,
	Default = 16,
	Callback = function(v) walkSpeed = v end    
})

PlayerTab:AddSlider({
	Name = "Zıplama Gücü",
	Min = 50,
	Max = 800,
	Default = 50,
	Callback = function(v) jumpPower = v end    
})

PlayerTab:AddSlider({
	Name = "Yerçekimi Ayarı",
	Min = 0,
	Max = 500,
	Default = 196,
	Callback = function(v) workspace.Gravity = v end    
})

PlayerTab:AddToggle({
	Name = "Noclip",
	Default = false,
	Callback = function(v) noclipActive = v end    
})

RunService.RenderStepped:Connect(function()
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = walkSpeed
        LocalPlayer.Character.Humanoid.JumpPower = jumpPower
    end
end)

ShopTab:AddDropdown({
	Name = "Otomatik Sandık Alımı",
	Default = "None",
	Options = {"None", "Common Chest", "Uncommon Chest", "Rare Chest", "Epic Chest", "Legendary Chest"},
	Callback = function(v) autoBuyType = v end    
})

task.spawn(function()
    while true do
        if autoBuyType ~= "None" then
            workspace.ItemBoughtFromShop:FireServer(autoBuyType, 1)
        end
        task.wait(1.5)
    end
end)

-- Anti-AFK
local vu = game:GetService("VirtualUser")
LocalPlayer.Idled:Connect(function()
    vu:Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
    task.wait(1)
    vu:Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
end)

OrionLib:Init()
